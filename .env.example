# Anthropic API key (Claude Sonnet 4.5)
ANTHROPIC_API_KEY=sk-ant-api03-...

# Model used for OCR extraction and chat
CLAUDE_MODEL=claude-sonnet-4-5

# Server
HOST=0.0.0.0
PORT=8700

# Login credentials
APP_USERNAME=Akshay
APP_PASSWORD=changeme

# Optional (deployment): where SQLite DB + uploads live (default: ./data)
# DATA_DIR=/app/data
# Optional (deployment): fixed session-signing secret so logins survive redeploys
# SESSION_SECRET=some-long-random-string
